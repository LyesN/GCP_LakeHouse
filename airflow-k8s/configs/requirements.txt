# Requirements pour Airflow avec support GCP
# Version compatible avec apache/airflow:2.8.1

# Providers GCP pour BigQuery, GCS, etc.
apache-airflow-providers-google>=10.10.0

# Providers pour bases de données
apache-airflow-providers-postgres>=5.6.0

# Providers pour Celery
apache-airflow-providers-celery>=3.3.0

# Librairies pour traitement de données
pandas>=1.5.0
numpy>=1.24.0

# Utilitaires
requests>=2.28.0
python-dateutil>=2.8.0

# Monitoring et observabilité
prometheus-client>=0.15.0

# Tests (optionnel)
pytest>=7.0.0
pytest-cov>=4.0.0

# Development tools (optionnel)
black>=22.0.0
flake8>=6.0.0